[project]
name = "kg-log-backend"
version = "0.1.0"
description = "Backend for KG-log application"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "httpx>=0.28.1",
]
