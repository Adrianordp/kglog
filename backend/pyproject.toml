[project]
name = "kg-log-backend"
version = "0.1.0"
description = "Backend for KG-log application"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.18.4",
    "fastapi>=0.135.1",
    "httpx>=0.28.1",
    "sqlalchemy>=2.0.48",
]
