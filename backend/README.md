# Kg-Log Backend

This is the backend service for the Kg-Log application, built with FastAPI.

## Run Instructions

1. Run the backend server:
```bash
run uvicorn app.main:app
```

2. Run with reload for development:
```bash
run uvicorn app.main:app --reload
```